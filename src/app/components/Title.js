import React from 'react';
import style from '../styles/Title.module.css';

const Title = () => {
	return (
		<div className={style.title}>
			<h1>Bible Verse Snippets</h1>
			<h2>Find and share your favorite verses!</h2>
		</div>
	);
};

export default Title;
